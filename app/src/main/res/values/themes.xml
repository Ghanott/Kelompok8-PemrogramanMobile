<resources>

    <style name="Base.Theme.AplikasiAyamGeprek" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Disable Material3 automatic button tint -->
        <item name="materialButtonStyle">@style/NoTintButtonStyle</item>

    </style>

    <!-- Style that removes tinting -->
    <style name="NoTintButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
        <item name="rippleColor">@android:color/transparent</item>
    </style>

</resources>
